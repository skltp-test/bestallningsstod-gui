{
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
	"cy:run:junit" : "cypress run --reporter mocha-junit-reporter",
    "cy:verify": "cypress verify",
    "cy:proxy:run": "node proxy/app.js | cypress run --reporter mocha-junit-reporter",
	"cy:proxy:open": "node proxy/app.js | cypress open"
  },
  "dependencies": {
    "cypress": "^3.7.0",
    "fs": "0.0.1-security",
    "http": "0.0.0",
    "http-proxy": "^1.18.1",
    "mocha": "^6.2.2",
    "mocha-junit-reporter": "^1.23.1",
    "morgan": "^1.9.1"
  }
}
